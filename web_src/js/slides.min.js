let e, n, t, i = "xClassSlides"; function l(l, o) { if (!o) return; n = o, l && (e = l); const r = document.createElement("button"); if (r.innerHTML = "Refresh", n.querySelector("h2").appendChild(r), t = document.createElement("div"), t.id = "slides-content", n.appendChild(t), r.addEventListener("click", (() => { !async function (e) { t.innerHTML = "<x-loading title='Fetching data'></x-loading>", await e.getSlides((e => { if ("ok" === e.status) { const n = JSON.stringify(e.datas); localStorage.setItem(i, btoa(n)), a() } else t.innerHTML = `<div> Something went wrong! 😢</div>\n            <ul>\n            <li> Status: ${e.status}</li>\n            <li> Pleas contact @x-kalux on Discord</li>\n            </ul>` })) }(l) })), localStorage.getItem(i)) return a(); const s = document.createElement("p"); s.innerHTML = "Data not found !\n    <ul>\n    <li> Click on 'Refresh' button.</li>\n    <li> Contact @x-kalux on Discord if data remains missing after refreshing.</li>\n    </ul>\n    ", t.appendChild(s) } function a() { const e = localStorage.getItem(i); if (e) { const n = JSON.parse(atob(e)), i = document.createElement("ul"); for (let e = 0; e < n.length; e++) { const t = n[e], l = document.createElement("li"); l.innerHTML = `<a href="${t.url}" target="_blank">${t.title}</a>`, i.appendChild(l) } t.innerHTML = "", t.appendChild(i) } else t.innerHTML = `<div> Something went wrong! 😢</div>\n        <ul>\n        <li> Status: ${response.status}</li>\n        <li> Pleas contact @x-kalux on Discord</li>\n        </ul>` } export { l as renderSlidesList }